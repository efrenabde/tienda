version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run:
          command: cp ./tienda-micros/users/requirements.txt /home/circleci/project/
          name: copy    
      - python/load-cache
      - python/install-deps
      - python/save-cache
      - run:
          command: ./tienda-micros/orders/application.py test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
